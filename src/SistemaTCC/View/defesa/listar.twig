{% extends 'layout.twig' %}
{% block content %}
	<div class="row">
    <div class="col-sm-6 col-xs-12">
      <h3>{{ titulo }}</h3>
    </div>
    <div class="col-sm-6 col-xs-12">
			<a href="./defesa/cadastrar" class="btn btn-success btn-cadastrar"><span class="glyphicon glyphicon-plus"></span> Agendar Defesa</a>
    </div>
	</div>
	<div class="row">
    <div class="col-xs-12">
      <table class="table table-striped">
        <thead>
          <tr>
            <th width="170px">TCC</th>
            <th>Aluno</th>
            <th>Data:</th>
            <th>Local:</th>
            <th></th>
          </tr>
          <tbody id="lista-js">
            {% for defesa in defesas %}
            <tr>
              <td>{{ defesa.getTcc().getTitulo() }}</td>
              <td>{{ defesa.getTcc().getAluno().getPessoa().getNome() }}</td>
              <td>{{ defesa.getDataHora()|date('d/m/Y H:i:s') }}</td>
              <td>{{ defesa.getLocal()}}</td>
              <td>
                <div class="pull-right">
                  <a href="./defesa/editar/{{ defesa.getId() }}" class="btn btn-primary btn-xs">
                    <i class="fa fa-pencil fa-lg" aria-hidden="true"></i>
                    <span class="hidden-xs">Editar</span>
                  </a>
                  <button data-id="{{ defesa.getId() }}" class="excluir btn btn-danger btn-xs" data-id="{{ defesa.getId() }}">
                    <i class="fa fa-trash-o fa-lg" aria-hidden="true"></i>
                    <span class="hidden-xs">Excluir</span>
                  </button>
                </div>
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </thead>
      </table>
    </div>
  </div>
{% endblock %}
{% block scripts %}
<script src="./app/defesa/listar.js"></script>
{% endblock %}